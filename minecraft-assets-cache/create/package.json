{
  "private": true,
  "dependencies": {
    "@actions/cache": "^4.0.3",
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.6",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "esbuild --platform=node --bundle index.mjs --outdir=dist --minify --sourcemap",
    "format": "prettier *.mjs --write"
  }
}
